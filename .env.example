# Frontend runs against an in-browser mock API by default, so `npm run dev`
# works with no backend. Flip this to false once the identity/vmaudit
# services are running behind the Vite proxy (see vite.config.ts).
VITE_USE_MOCKS=true
